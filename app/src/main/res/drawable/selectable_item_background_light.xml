<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- TODO: Extract into themes. ?attr/* doesn't work in here prior to a certain API level, so instead
               will need to have a different selector for each style, and use ?attr/this_drawable as the background
               for the items in question -->
    <item android:drawable="@color/orange_4" android:state_pressed="false" android:state_selected="true" />
    <item android:drawable="@color/colorPrimary" android:state_selected="false" />
</selector>